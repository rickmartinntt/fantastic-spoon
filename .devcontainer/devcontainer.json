{
  "containerEnv": {
    "VITE_STORAGE_ACCOUNT": "${{ codespaceSecrets.VITE_STORAGE_ACCOUNT }}",
    "VITE_STORAGE_SAS": "${{ codespaceSecrets.VITE_STORAGE_SAS }}",
    "COSMOS_ENDPOINT":  "${env:COSMOS_ENDPOINT}",
    "COSMOS_KEY":  "${env:COSMOS_KEY}",
    "COSMOS_DB":   "LoanParticipation",      // non-secret, can stay here
    "CON_QUERIES": "Queries"
    
  }
}